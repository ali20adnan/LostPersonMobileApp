import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF8B5CF6); // Violet
  static const primaryDark = Color(0xFF7C3AED);
  static const primaryLight = Color(0xFFA78BFA);

  // Accent colors
  static const accent = Color(0xFFD946EF); // Fuchsia
  static const accentDark = Color(0xFFC026D3);
  static const accentLight = Color(0xFFF0ABFC);

  // Background colors
  static const background = Color(0xFFFAFAFA);
  static const backgroundDark = Color(0xFF1A1A1A);
  static const surface = Colors.white;
  static const surfaceDark = Color(0xFF2D2D2D);

  // Text colors
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF6B7280);
  static const textLight = Color(0xFF9CA3AF);
  static const textOnPrimary = Colors.white;

  // Status colors
  static const success = Color(0xFF10B981);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
  static const info = Color(0xFF3B82F6);

  // Connection status
  static const connected = Color(0xFF10B981);
  static const disconnected = Color(0xFFEF4444);
  static const connecting = Color(0xFFF59E0B);

  // Divider and border
  static const divider = Color(0xFFE5E7EB);
  static const border = Color(0xFFD1D5DB);

  // Record button
  static const recordIdle = Color(0xFF9CA3AF);
  static const recordActive = Color(0xFFEF4444);
  static const recordProcessing = Color(0xFF8B5CF6);
}
